const PENDING = 'pending';
const RESOLVED = 'resolved';
const REJECTED = 'rejected';

class Promise {
  // excutor传入的被执行函数
  constructor(excutor) {
    const self = this;
    self.status = PENDING; // 初始状态时pending
    sel;
  }
}
